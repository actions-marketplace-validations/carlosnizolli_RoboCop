name: 'Robot Framework RoboCop'
description: 'Run Robocop'
branding:
  icon: 'check-square'  
  color: 'green'
  
inputs:
  warnings:
    description: 'Warning Max number'
    required: true
    
  
runs:
  using: "docker"
  image: "Dockerfile"

     
